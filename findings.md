# 发现与决策

## 需求
- P0：前端雷达从 6 维改为 4 维；三处能力边界说明；弱标签正面答辩话术；数据来源四分类统计。
- P1：从两套年鉴提取 26 县 2020–2022 的灾害候选事件，只生成候选 JSON 和状态清单。

## 研究发现
- `credit_decision.py` 是唯一授信决策纯计算链；用户要求使用四维权重：工商 0.40、生态 0.30、气象 0.20、经营 0.10。
- `models.py` 当前统计把观测来源白名单汇为 `real_rows`，未对业务、派生、样例做面向页面的四分类；真实事件融合目前仅接受 `source_url`。
- `real_labels_1500.json` 的未来源月份含历史“无公开报道即无灾”遗留文本；本次不触碰标签数据。
- `risk_event_labels.json` 已区分 `is_real_label`、`is_sample`；历史事件另存在 `risk_event_labels_historical.json`。

## 技术决策
| 决策 | 理由 |
|---|---|
| 在 `models.py` 提供明确四类行数与比例字段，同时保留旧字段兼容 | 前端需要准确呈现且降低现有接口破坏风险 |
| 三个脚注放在各自模块标题/说明附近 | 降低“灾害预测”被误读为确定性预测的风险 |
| 候选事件使用 `source_kind=yearbook`、`verified=true`、空 URL | 用户预先规定纸本年鉴凭证格式 |

## 遇到的问题
| 问题 | 解决方案 |
|---|---|
| 前端文件过大无法全文读取 | 只检索目标关键词并按定位行段读取 |
