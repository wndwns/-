# 工银牧融项目深化优化方案（实施前待批准）

> 形成日期：2026-08-07
> 文档状态：方案讨论中，尚未获得最终实施批准
> 适用项目：`C:\Users\WH\Desktop\gonghangbei - 副本`
> 当前正式版本：`backend/server.py` + `frontend/`
> 远端仓库：`https://github.com/wndwns/-.git`
> 当前项目事实基线：`项目全景说明（Codex读）.md`

## 1. 本文的作用

本文只固化截至 2026-08-07 用户已经明确确认的深化方案，防止后续讨论或实施时遗漏、误解或偏离原项目方向。

本文不是开工指令。当前只完成了产品方向、数据边界、业务主链和两项架构决策；代码质量、测试、性能与失败处理的工程评审尚未结束。必须继续完成剩余方案讨论，并由用户对完整方案作最终批准后，才能修改业务代码。

以下规则持续有效：

1. 不删除、不隐藏现有功能；如后续确需砍功能，必须先单独询问用户。
2. 任何实质影响功能、数据、交互、成本、权限、安全或验收标准的决定，都必须先由用户确认。
3. 样例、假设、派生值和待核验资料必须明确标注，不能宣传成真实工行业务数据。
4. 实施完成后只提交本次改动并推送 GitHub，不混入用户已有的未提交文件。
5. 保留旧版保护分支 `codex/backup/pre-deepening-20260807`，不得覆盖或删除。

## 2. 方案是否已经讨论完

尚未全部讨论完。

### 2.1 已经确认的部分

- 深化定位和比赛叙事。
- 主案例、资料不足案例及其他现有功能的关系。
- 生态压力进入授信测算的业务路径。
- 草场面积、草畜参数及数据来源的处理原则。
- 12 个月现金流、贷款金额和授信敞口的总体框架。
- 基准、雪灾压力、复合极端三类情景。
- 保险资料的使用边界。
- 贷后核查任务及人工决策边界。
- 默认工作台和 14 个现有页面的保留原则。
- 案例数据独立存放和无状态测算接口两项架构决策。

### 2.2 还需要继续确认的部分

- 计算模块内部边界、字段命名和错误信息规范。
- 每个输入字段的类型、单位、允许范围、必填条件和缺失处理。
- 草畜平衡所采用的具体标准、文献、系数范围及地区适用性。
- 现金流各项的精确计算口径和区间传播方法。
- 前端工作台的具体布局、交互顺序和结果表达。
- 最小测试矩阵、关键边界案例和验收样例。
- 性能、文件损坏、数据缺失、接口异常等失败场景。
- 最终实施文件清单、分阶段顺序和完整验收标准。

## 3. 核心定位

### 3.1 已确认的新核心表达

**生态压力进入主体现金流，再辅助测算最高授信敞口和贷后核查。**

这不是把项目改成另一个方向，而是把原有主线中“草场风险、饲草成本、贷款金额、贷后预警”之间缺失的业务传导补齐。

### 3.2 “生态压力进入主体现金流”的准确含义

生态风险不能直接乘一个系数得到贷款金额。正确链条是：

```text
气象与遥感证据
  -> 草场供给和可利用条件变化
  -> 主体饲草缺口变化
  -> 外购饲草、运输等经营成本变化
  -> 12 个月经营现金流变化
  -> 融资需求和偿债能力变化
  -> 推荐新增授信上限与最高授信敞口
  -> 需要人工执行的贷后核查任务
```

例如，雪灾压力不是直接让授信额度打八折，而是先影响某些月份的可利用草场或补饲需求，随后增加这些月份的现金支出。系统再根据实际资金缺口、还款能力和其他授信约束计算结果。

### 3.3 明确不采用的旧思路

- 不再用生态风险分直接乘授信额度。
- 不再用单一县域风险系数直接得出单户贷款金额。
- 不再以“保险覆盖率达到某阈值”直接给额度乘数。
- 不把随机森林或 Ridge 的弱标签拟合指标包装成违约预测能力。
- 不让一个环境信号直接触发压降额度、停贷、收贷或风险分类。

## 4. 比赛中要做深的内容

项目的“深”不靠增加更多页面或模型名，而靠把一条高原牧区信贷决策链做透：

1. **生态证据可追溯**：每个环境输入带来源、时间、单位、状态和限制。
2. **草畜关系可解释**：草场面积、可利用面积、牲畜负荷和参数范围共同形成饲草供需区间。
3. **经营影响可计算**：饲草缺口只通过成本或收入变化进入主体月度现金流。
4. **贷款金额有约束**：贷款建议不再是风险系数乘法，而是多项业务上限取最小值。
5. **贷前贷后一体化**：压力月份、证据缺口和关键阈值能够转化为人工核查任务。
6. **系统知道何时不能算**：资料不足时主动停止给金额，输出缺失材料和下一步动作。

## 5. 案例与对象范围

### 5.1 主案例

使用 **班戈县绿色牧业合作社** 作为完整主案例。

- 案例性质：结构化、脱敏的比赛样例。
- 数据状态：`sample`、`sample_assumption` 或其他明确的非真实标记。
- 用途：完整展示生态证据、草畜平衡、月度现金流、贷款金额、授信敞口和贷后核查。
- 禁止表述：不得暗示该合作社数据来自真实工行客户、真实授信档案或真实合同。

### 5.2 资料不足案例

百巴村作为 **数据不足时拒绝计算和启动尽调** 的控制案例。

- 当前可用资料主要是 21 户主体与 1135 头牦牛的资产/耳标登记参考。
- 不得把这些登记记录当成已核验保险合同、授信记录或还款记录。
- 缺少合同和金融资料时，不输出贷款金额、保险增信效果或综合信用结论。
- 系统应输出资料缺口和人工核验清单。

### 5.3 其他主体与现有模块

- 其他主体类型继续保留，不强行套用完整合作社现金流公式。
- 当前 14 个逻辑页面全部保留。
- 保险协同、产业链、绿色绩效、边疆民生、合作社排序、资产画像和灾害预测继续作为证据或辅助能力存在。
- 任何删除、隐藏、合并或降级必须另行获得用户批准。

## 6. 数据设计原则

### 6.1 数据状态

所有进入主案例的关键字段必须标记来源状态，至少区分：

| 状态 | 含义 | 能否直接用于样例测算 |
|---|---|---|
| `public_observation` | 可追溯公开环境观测 | 可以，但需保留来源和时间 |
| `derived` | 由公开观测或规则计算 | 可以，但需显示公式和限制 |
| `sample` | 为比赛构造的主体业务样例 | 可以，必须明确不是现实客户数据 |
| `sample_assumption` | 系统内置的合理假设 | 可以，必须允许识别和修改 |
| `user_provided_unverified` | 用户本次输入、尚未外部核验 | 可以用于情景试算，不能标为已核验事实 |
| `policy_assumption` | 演示使用的利率、期限或政策假设 | 可以，必须可编辑且不能冒充工行正式政策 |
| `missing` | 缺失关键材料 | 按规则阻断相关结论 |

具体枚举名称还需在代码质量评审中确认；本表先固定业务语义。

### 6.2 草场面积

主体草场面积不能从县域草原总面积推算。

已确认的主体输入包括：

- 总经营草场面积。
- 冬季可利用草场面积。
- 禁牧、休牧或不可利用面积。
- 夏季草场面积，作为可选项。

处理规则：

- 系统提供一组合理的主案例默认值，标记为 `sample_assumption`。
- 页面保留用户输入位置，用户填写后标记为 `user_provided_unverified`。
- 以后获得的县域草原总面积只作区域背景和合理性参照，不反推主体面积。
- 如未来接入真实确权或承包资料，必须取得权利人或合作社授权，并单独核验数据使用权限。

### 6.3 草畜参数

- 参数采用国家或地方标准、官方资料、同行评议研究中的适用范围。
- 参数必须带单位、来源、适用地区、适用时间和限制说明。
- 用户可以覆盖默认参数，覆盖后标记为假设。
- 结果以区间呈现，避免用不可靠参数生成虚假的单点精度。
- 当前 `NPP * 15000 + 5000` 不能直接作为主体绝对载畜能力。
- 当前饲草计算中的固定日粮和规模折扣不能未经校准直接进入新授信模型。

### 6.4 保险数据

只有在真实合同要素完成核验后，保险才能作为现金流损失缓释或其他信贷约束的证据。

需要核验的材料至少包括保险公司、保单号、被保险主体、标的、责任范围、保额、免赔、期限和理赔记录。未完成核验时，保险只能形成资料缺口或核查任务，不能直接放大授信额度。

## 7. 十二个月现金流

### 7.1 时间粒度

主案例采用连续 12 个月的月度现金流，不采用只看全年合计的简化表。

### 7.2 生态压力进入现金流的位置

生态信号只允许通过可解释的经营项目进入现金流一次，主要路径为：

```text
草场可供给区间
  与
牲畜饲草需求区间
  -> 饲草缺口区间
  -> 外购饲草及相关增量成本区间
  -> 对应月份经营净现金流区间
```

不得在现金流已经增加成本后，再用生态风险系数对授信额度二次折减，避免重复计量同一风险。

### 7.3 月度结果

每个情景至少输出：

- 月度饲草缺口区间。
- 月度增量经营成本区间。
- 月度经营净现金流区间。
- 最大月度资金缺口。
- 首个出现明显压力的月份。

月度收入、常规成本、现有债务和期初现金等字段的精确口径尚待后续评审确认。

## 8. 贷款金额与授信敞口

### 8.1 推荐新增贷款金额

已确认的总体框架为：

```text
推荐新增贷款金额上限 = min(
  实际融资需求,
  现金流可支持上限,
  统一授信剩余额度,
  产品或政策上限,
  抵质押支持上限（适用时）
)
```

这五项中任一项缺少关键资料时，应根据资料重要性返回受限结果或“暂不可测算”，不能自行补成真实值。

### 8.2 最高授信敞口

```text
最高授信敞口 = 当前有效融资余额 + 推荐新增贷款金额上限
```

“最高”表示在当前输入和情景下的辅助测算上限，不是自动审批结果，也不是工行正式授信承诺。

### 8.3 贷款使用和还款结构

主案例采用：

- 根据饲草采购节奏分阶段提款。
- 按月计息。
- 在牲畜销售或经营回款季安排本金分期偿还。
- 利率、提款月份、还款月份、期限和其他产品条件均标记为 `policy_assumption`。
- 所有政策假设允许调整，不宣传为工行真实产品条款。

## 9. 压力情景

系统提供三种统一情景：

1. **基准情景**：使用主案例常态参数和对应月份的常态环境条件。
2. **雪灾压力情景**：重点改变积雪、可利用草场或补饲压力相关输入。
3. **复合极端情景**：组合多个对经营现金流不利的环境变化。

情景冲击尽量使用历史分位数或可追溯范围构造。没有经过校准时，不给这些情景标注发生概率，也不称为统计置信情景。

三个情景使用同一组输出：

- 饲草缺口区间。
- 增量成本区间。
- 最大月度资金缺口。
- 推荐新增贷款金额上限。
- 最高授信敞口。
- 首个风险月份。
- 贷后核查动作。

## 10. 贷后核查

### 10.1 自动化边界

系统只自动生成有证据支持的人工调查任务，不自动执行金融处置。

单一环境信号不得直接触发：

- 降低授信额度。
- 停止提款或停贷。
- 提前收贷。
- 调整风险分类。
- 自动拒绝客户。

### 10.2 任务信息

每项贷后任务应包含：

- 主体和贷款关联信息。
- 触发原因、证据来源、观测日期和阈值。
- 严重程度。
- 待补或待核验材料。
- 负责人和完成时限。
- 系统建议。
- 客户经理或管理人员决定。
- 复核人员。
- 处理结果。
- 下次核查日期。
- 任务状态。

具体状态枚举、权限和写入方式尚未确认。本轮已确认的无状态接口只覆盖授信测算，不代表贷后任务写入功能已经获批。

## 11. 前端工作台

### 11.1 默认入口

第一屏调整为 **授信与贷后工作台**，优先服务工行客户经理完成主体选择、证据核验、现金流分析、贷款测算和贷后任务查看，而不是使用营销型首页作为主要入口。

### 11.2 导航分组

现有功能按以下三组组织：

1. 核心业务。
2. 生态证据。
3. 辅助能力。

这只是信息架构重组，不等于删除或隐藏任何页面。具体菜单归属和页面布局尚待 UX 方案确认。

### 11.3 主工作台的核心顺序

```text
选择主体
  -> 查看数据状态和缺失材料
  -> 检查草场及经营输入
  -> 比较三个现金流情景
  -> 查看新增贷款上限和最高敞口
  -> 查看依据、限制和贷后核查任务
```

页面必须明显区分“已观察事实、派生结果、样例假设、用户未核验输入、政策假设和缺失材料”。

## 12. 已确认的技术架构

### 12.1 案例数据独立存放

新增独立的 `credit_cases` 逻辑表，并沿用现有 JSON Store。

计划中的数据文件为：

```text
backend/data_store/credit_cases.json
```

理由：

- 主案例包含草场、牲畜、月度经营、融资和情景参数，结构比通用主体表复杂。
- 不把大量样例字段塞入 `business_subjects.json` 和 `finance_credit.json`。
- 继续复用当前 `backend/store.py` 的 JSON 存储方式，不新增数据库或依赖。

### 12.2 无状态测算接口

计划新增：

```http
POST /api/credit-decision/evaluate
```

请求处理链：

```text
credit_cases.json 基础案例
  -> 合并本次请求中的用户输入
  -> 校验字段、单位和允许范围
  -> 执行草畜、现金流和授信测算
  -> 返回结果、数据状态、限制和缺失材料
```

已确认的行为：

- 用户本次输入不持久化，刷新后恢复样例默认值。
- 缺少关键资料时返回阻断状态和缺失材料清单。
- 格式错误或超出允许范围时返回明确校验错误。
- JSON 案例文件缺失或损坏时，不回退到伪造计算结果。
- 不增加登录、账号、数据库写入或并发编辑能力。

### 12.3 预计最小改动边界

以下仅是当前架构评审中的预计触点，不是最终批准的文件清单：

- `backend/store.py`：登记 `credit_cases` 表。
- `backend/data_store/credit_cases.json`：主案例样例。
- 新的后端纯计算模块：文件名和内部接口待确认。
- `backend/server.py`：增加测算 API。
- `frontend/app.js`、`frontend/index.html`：接入工作台和新测算结果。
- 测试文件：位置和范围待测试评审确认。

不计划引入 Redis、消息队列、微服务、新数据库、新前端框架或状态管理库。

## 13. 专家评审形成的约束

青藏高原畜牧、农业信贷和模型竞赛三个角度的评审形成了以下共同意见，并已被本方案吸收：

- 方向有价值，但当前公式不足以支持真实信贷模型。
- 项目应定位为透明的情景传导和辅助决策，不宣传因果或违约预测。
- 当前 NPP 换算不能直接代表主体绝对载畜能力。
- 饲草生物需求不能因为采购规模变大而直接打折。
- 生态影响应只进入现金流一次，防止重复扣减授信。
- 保险必须先核验合同，再讨论损失缓释。
- 弱标签模型指标不能当成真实业务有效性证据。
- 资料不足时停止计算，比输出伪精确金额更可信。

## 14. 本轮明确不做的内容

- 不开发自动审批、自动拒贷或自动金融处置。
- 不宣称系统已接入真实工行客户、授信、逾期或核销数据。
- 不把百巴村资产登记包装成保险合同数据。
- 不为比赛演示强行建设生产级账号、权限、审计和数据库体系。
- 不重新训练一个号称预测违约的机器学习模型。
- 不删除现有 14 个页面或其他原有功能。
- 不为“技术感”增加无实际需要的框架、服务或依赖。

## 15. 实施前必须补齐的评审

### 15.1 代码质量评审

需要确认：

- 计算模块的最小职责边界。
- 输入和输出字段的统一命名。
- 金额、面积、重量、时间等单位的统一规则。
- 金额精度、区间表达和舍入规则。
- 现有 `creditDecisionBrief` 中哪些展示保留、替换或标记为历史口径。

其中如涉及删除、隐藏或改变现有功能，必须单独获得用户批准。

### 15.2 测试评审

需要确认至少覆盖：

- 基准情景完整计算。
- 雪灾和复合极端情景的单向传导。
- 缺少关键材料时阻断结果。
- 非法面积、负金额、错误月份和单位错误。
- 不重复计入生态风险。
- 保险未核验时不产生额度增益。
- 最高授信敞口不突破五项约束的最小值。
- 百巴村不产生贷款金额。

### 15.3 性能与失败场景评审

需要确认：

- JSON 案例缺失、为空或损坏时的响应。
- 前端请求超时或接口错误时的可见提示。
- 多次调整输入时是否需要防抖或取消旧请求。
- 计算量和响应时间验收目标。
- 是否需要缓存；没有测量证明前默认不增加缓存。

### 15.4 UX 评审

需要确认：

- 14 个页面分别归入哪一导航组。
- 第一屏具体信息密度和操作顺序。
- 三种情景的对比形式。
- 输入来源状态、计算限制和缺失材料的视觉表达。
- 移动端与答辩投屏下的布局验收。

## 16. 预期验收结果

最终实现至少应让答辩者用一个连续流程证明：

1. 选择班戈县绿色牧业合作社。
2. 看见草场、牲畜、经营和融资输入及其数据状态。
3. 调整主体草场面积或其他允许输入。
4. 比较基准、雪灾压力和复合极端三种情景。
5. 看到生态压力如何改变具体月份的饲草缺口与经营成本。
6. 看到 12 个月现金流、最大月度资金缺口和首个压力月份。
7. 看到推荐新增贷款金额上限由五项约束共同决定。
8. 看到最高授信敞口和对应限制说明。
9. 看到系统生成的是人工贷后核查任务，而不是自动金融处置。
10. 切换到百巴村后，系统因资料不足停止金额计算并列出缺失材料。

具体数值、页面视觉效果和响应时间标准需要在剩余评审完成后补充，并由用户最终确认。

## 17. Git、回退与实施门槛

### 17.1 Git 规则

- 远端使用 `https://github.com/wndwns/-.git`。
- 旧版保护分支 `codex/backup/pre-deepening-20260807` 保持不变。
- 实施时建立 `codex/` 前缀的独立开发分支。
- 只暂存本次明确修改的文件。
- 不提交 `.env`、密钥、会话文件、个人信息或用户无关改动。
- 完成验证后正常提交并推送 GitHub，以便回退和比较。

### 17.2 开工门槛

只有同时满足以下条件才能开始修改业务代码：

1. 代码质量、测试、性能/失败场景和 UX 评审结束。
2. 所有重大选择已逐项得到用户确认。
3. 本文更新为完整方案和可执行验收标准。
4. 用户明确回复批准完整方案并同意开始实施。

在此之前，只能继续分析、讨论和完善方案文档。

## 18. 当前结论

当前已经确定的不是“直接开始改代码”，而是一条清晰的深化主线：

**用高原生态证据解释合作社未来 12 个月的饲草与现金流压力，再在真实融资需求、现金流承受能力、统一授信余额、产品政策和担保条件的共同约束下测算新增贷款上限，同时把关键压力和资料缺口转化为人工贷后核查任务。**

这条主线保留了项目原有的生态、贷款金额、保险和贷后方向，但把它们从并列展示深化为可追溯、可解释、会在资料不足时主动停止的业务闭环。
